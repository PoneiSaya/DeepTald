import 'package:flutter/material.dart';

/** questa pagina e un placeholderr non deve neanche stare in questo package */
class UsaIaScreen extends StatelessWidget {
  const UsaIaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
